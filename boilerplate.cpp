#include<bits/stdc++.h>//HEADER File that includes all standard library
#include<iostream>
#include<vector>
using namespace std;
#define rep(i,a,b) for(int i =a; i<b; i++)
#define v_i vector<int>

/* this code is written by ratnesh t pasi */

class solution
{
    public:
        int response(/* args */) {
    
        }
};

int main()
{
    int times;
    cin>>times;
    
    while(times--) {
        solution obj;
        int ans = obj.response(/* parms */);
        cout<<ans;
    }
    return 0;
}